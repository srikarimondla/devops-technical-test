pipeline {
    agent {
       label 'java-docker-slave'
    }
    stages {
        stage('Build') { 
            steps {
                sh 'chmod +x mvnw'
                sh './mvnw clean package' 
                sh 'ls -ll'
            }
        }
    }
}
